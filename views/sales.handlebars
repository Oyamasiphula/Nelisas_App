<div class="row"> 
    <div class="col-xs-7 col-md-7 ">
        <table class="table table-bordered">
            <thead class="label-info">
        
                <tr>
                    <th><h3>Product Name</3></th>
                    <th><h3>Product Price</3></th>
                    <!-- <th><h3>Manage</3></th> -->
                </tr>
            </thead>
        {{#Sales}} 
            <tbody>
            <tr>
        <td name="Product_name">{{Product_name}}
        </td>
            <td name="product_price">R{{Tot_Earnings_Per_Product}}
            </td>
        <!-- <td>
         <button type="button" class="btn btn-default" onClick="location.href='/products/edit/{{id}}'"><span class="glyphicon glyphicon-edit"></span> Edit</button>
         <button type="button" class="btn btn-danger" onClick="location.href='/products/delete/{{id}}'"><span class="glyphicon glyphicon-remove"></span> Remove</button>
        </td> -->
            </tr>
            </tbody>
        {{/Sales}}
            </table>
            <div><span class="glyphicon glyphicon-plus"></span><button type="button" class="btn btn-success" onClick="location.href='products/add'">+ Add new product +</button></div>   
        </div>
    </div>

        <style type="text/css">
       

td, th {
    padding: 0;
    color: floralwhite;
    font-variant: small-caps;
    text-align: center;
    font-size: x-large;
}
        </style>